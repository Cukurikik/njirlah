{
  "buildCommand": "pnpm --filter @workspace/njirlah-ai run build",
  "outputDirectory": "artifacts/njirlah-ai/dist",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" }
  ]
}

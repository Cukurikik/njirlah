kind = "web"
previewPath = "/"
title = "DevStudio IDE"
version = "1.0.0"
id = "artifacts/devstudio"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24554

[services.development]
run = "pnpm --filter @workspace/devstudio run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/devstudio", "run", "build" ]
publicDir = "artifacts/devstudio/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24554"
BASE_PATH = "/"
